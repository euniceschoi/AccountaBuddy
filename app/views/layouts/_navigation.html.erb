<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1 id="home-nav-bar"><%= link_to 'AccountaBuddy', user_path(current_user.id) %></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <div id="right-nav" style="float:right;margin-right:2em;"> 
      <span style="color:white"> <%= link_to 'Profile', user_path(current_user.id) %> | <%= link_to "Start a new goal", categories_path %> | <%= link_to 'Logout', '/logout' %> | <%= link_to 'Edit Account', edit_user_path(current_user.id) %></span>
    </div>
  </section>
</nav>
